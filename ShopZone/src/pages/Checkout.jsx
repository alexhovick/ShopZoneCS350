const Checkout = () => (
    <div className="font-bold text-3xl text-white text-center">
        Checkout: I mean even this part, i still have no clue how to do
    </div>
);

export default Checkout;